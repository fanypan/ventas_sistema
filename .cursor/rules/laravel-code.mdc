---
description: Capas Laravel pragmáticas — controllers delgados, FormRequests, Actions/Services, scopes Eloquent en modelos
globs: app/**/*.php,Modules/**/*.php
alwaysApply: false
---

# Capas Laravel (pragmático)

Controller delgado + FormRequest + Action/Service. Eloquent vive en la Action/Service (o en CRUD chico). **No** Clean Architecture hexagonal: ni repositorio genérico por modelo ni entidades paralelas a Eloquent.

## Qué va dónde

- **Controller**: HTTP. Autoriza (middleware / `AuthorizesCrud`), type-hint del FormRequest, llama **una** Action/Service, responde. Cómo responder: `.cursor/rules/http-responses.mdc`. Sin `$request->validate`, sin `DB::transaction`, sin reglas de stock/caja. Return type explícito en cada método.
- **FormRequest**: reglas + mensajes. `authorize()` solo si no alcanza el middleware. `validated()` es el contrato de entrada.
  - Módulos: `Modules/{Name}/Http/Requests/StoreFooRequest.php`
  - Plataforma: `app/Http/Requests/Platform/`
  - Tenant (app): `app/Http/Requests/`
- **Action**: un caso de uso de escritura con transacción o efectos (`execute(...)`). Ej. `Modules/Sales/Actions/ProcessSale.php`.
- **Service**: varios métodos que comparten invariantes. Ej. `CartService`, `SubscriptionService`.
- **Eloquent**: persistencia en Action/Service. Listados simples (`::paginate`) pueden quedar en el controller. Filtros que se repiten van en **scopes** del modelo (ver abajo).
- **Support**: funciones puras sin I/O (`app/Support`, `parse_currency`). No un cajón `Utils`.

CRUD chico (alta de cliente/proveedor): FormRequest + `Model::create($request->validated())` en el controller. No inventar un Service de 10 líneas.

## No hacer

- `FooRepository` que envuelve `Foo::` (en `config/modules.php` `repository.generate` ya está en `false`)
- Entidades de dominio duplicando models
- God-service que absorba todo un controller
- Cambiar contratos JSON del POS sin actualizar el JS y los tests
- `response()->json()` con arrays de Eloquent: usá `JsonApiResource` (ver `http-responses.mdc`)
- Mezclar este refactor con upgrade de Laravel

## Modelos: scopes Eloquent

Desde ahora, no repetir `where('status', 1)` / caja abierta / plan activo en controllers. El filtro vive en el modelo.

Atributo `#[Scope]` de Laravel 13. El método **no** se llama `scopeActive`: se llama `active`. El prefijo `scope` solo hace falta en el estilo viejo (`public function scopeActive()`).

```php
use Illuminate\Database\Eloquent\Attributes\Scope;
use Illuminate\Database\Eloquent\Builder;

#[Scope]
protected function active(Builder $query): void
{
    $query->where('status', 1);
}

// Product::active()->orderBy('description')->get()
// Caja::open()->where('user_id', $id)->first()
```

- Método `protected`, mutá `$query`. Parámetros después de `$query` si hace falta.
- Encadená: `Plan::active()->listedOnLanding()`.
- No uses global scopes para catálogo activo / caja abierta.
- Catálogo (`Product`, `Customer`, …): `active()`. Caja: `open()`. Venta: `paid()` / `credit()`. Tenant: `active()` / `billable()`.

## Estilo

- Una responsabilidad por método. Si un método valida + transacciona + manda mail, extraé.
- Clases/métodos en inglés; textos de UI en rioplatense.
- Errores de negocio: `App\Exceptions\BusinessRuleException` (o `ValidationException`). Web: `back()` + flash. JSON: `JsonEnvelope` (`http-responses.mdc`). Los 401/403/404/422 JSON del framework también salen envelope.
- FE: HTTP a la API **después** del `DB::commit`, dentro de la Action de cierre, no en el controller.
